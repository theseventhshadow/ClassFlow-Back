spring:
  datasource:
    url: jdbc:postgresql://message-db:5432/message_db
    driver-class-name: org.postgresql.Driver
    username: message_user
    password: message_pass
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    defer-datasource-initialization: false
  flyway:
    locations: classpath:db/migration
    enabled: true
    baseline-on-migrate: false
    out-of-order: false
  h2:
    console:
      enabled: false

server:
  port: 8084